var comp = app.project.activeItem;

var selLayer = comp.selectedLayers[0];

if(!(selLayer.source instanceof CompItem)){

}

function getSelLayer(){
  
}

function createProxy(type){
  if(type == "compLayer"){
    selLayer = getSelLayer();
  }
}
function setProxy(){

}
